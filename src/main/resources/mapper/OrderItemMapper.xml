<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gregori.order_item.mapper.OrderItemMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="com.gregori.order_item.domain.OrderItem">
        INSERT INTO order_items (order_id, order_count, item_id, item_name, item_price, status)
        VALUES (#{orderId}, #{orderCount}, #{itemId}, #{itemName}, #{itemPrice}, #{status})
    </insert>

    <update id="deleteByIds" parameterType="map">
        DELETE FROM order_items
        WHERE id IN (
        <foreach collection="list" item="item" separator=",">
            #{item}
        </foreach>
        )
    </update>

    <select id="findByOrderId" resultType="com.gregori.order_item.domain.OrderItem" parameterType="Long">
        SELECT id, order_id, order_count, item_id, item_name, item_price, status
        FROM order_items
        WHERE order_id = #{orderId}
    </select>

    <select id="findByIds" resultType="com.gregori.order_item.domain.OrderItem" parameterType="List">
        SELECT id, order_id, order_count, item_id, item_name, item_price, status
        FROM order_items
        WHERE id IN
        <foreach collection="itemIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
</mapper>
