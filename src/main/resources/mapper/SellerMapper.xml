<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gregori.seller.mapper.SellerMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="com.gregori.seller.domain.Seller">
        INSERT INTO sellers (member_id, business_number, business_name, status)
        VALUES (#{memberId}, #{businessNumber}, #{businessName}, #{status})
    </insert>

    <update id="update">
        UPDATE sellers
        SET business_number = #{businessNumber}, business_name = #{businessName}, status = #{status}
        WHERE id = #{id}
    </update>

    <update id="deleteByIds" parameterType="map">
        DELETE FROM sellers
        WHERE id IN (
        <foreach collection="list" item="item" separator=",">
            #{item}
        </foreach>
        )
    </update>

    <select id="findById" resultType="com.gregori.seller.domain.Seller" parameterType="Long">
        SELECT id, member_id, business_number, business_name, status, created_at, updated_at
        FROM sellers
        WHERE id = #{id}
    </select>

    <select id="findByMemberId" resultType="com.gregori.seller.domain.Seller" parameterType="Long">
        SELECT id, member_id, business_number, business_name, status, created_at, updated_at
        FROM sellers
        WHERE member_id = #{memberId}
    </select>
</mapper>
