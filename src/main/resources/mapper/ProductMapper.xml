<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gregori.product.mapper.ProductMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="com.gregori.product.domain.Product">
        INSERT INTO products (seller_id, category_id, name, price, inventory, status)
        VALUES (#{sellerId}, #{categoryId}, #{name}, #{price}, #{inventory}, #{status})
    </insert>

    <update id="update">
        UPDATE products
        SET category_id = #{categoryId}, name = #{name}, price = #{price}, inventory = #{inventory}, status = #{status}
        WHERE id = #{id}
    </update>

    <update id="deleteByIds" parameterType="map">
        DELETE FROM products
        WHERE id IN (
        <foreach collection="list" item="item" separator=",">
            #{item}
        </foreach>
        )
    </update>

    <select id="findById" resultType="com.gregori.product.domain.Product" parameterType="Long">
        SELECT id, seller_id, category_id, name, price, inventory, status, created_at, updated_at
        FROM products
        WHERE id = #{id}
    </select>

    <select id="findByIds" resultType="com.gregori.product.domain.Product" parameterType="List">
        SELECT id, seller_id, category_id, name, price, inventory, status, created_at, updated_at
        FROM products
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="findByKeyword" resultType="com.gregori.product.domain.Product">
        SELECT id, seller_id, category_id, name, price, inventory, status, created_at, updated_at
        FROM products
        WHERE name LIKE CONCAT('%', #{keyword}, '%')
        <include refid="orderBy"/>
        <include refid="limitAndOffset"/>
    </select>

    <select id="findByCategoryId" resultType="com.gregori.product.domain.Product">
        SELECT id, seller_id, category_id, name, price, inventory, status, created_at, updated_at
        FROM products
        WHERE category_id = #{categoryId}
        <include refid="orderBy"/>
        <include refid="limitAndOffset"/>
    </select>

    <select id="findBySellerId" resultType="com.gregori.product.domain.Product" parameterType="Long">
        SELECT id, seller_id, category_id, name, price, inventory, status, created_at, updated_at
        FROM products
        WHERE seller_id = #{sellerId}
        <include refid="orderBy"/>
        <include refid="limitAndOffset"/>
    </select>

    <sql id="limitAndOffset">
        <if test="limit != null">
            LIMIT #{limit}
        </if>
        <if test="offset != null">
            OFFSET #{offset}
        </if>
    </sql>

    <sql id="orderBy">
        <choose>
            <when test="sorter == 'CREATED_AT_DESC'">
                ORDER BY created_at DESC
            </when>
            <when test="sorter == 'PRICE_DESC'">
                ORDER BY price DESC
            </when>
            <when test="sorter == 'PRICE_ASC'">
                ORDER BY price ASC
            </when>
        </choose>
    </sql>
</mapper>
